terraform {
  backend "s3" {
    # region         = var.aws_region
    # key            = "{ key }"
    # bucket         = "tfst-{ key }"
    # dynamodb_table = "tflk-{ key }"
    # encrypt        = true
  }
}
